# Project Translator CLI

CLI for Project Translator.

## Install

```bash
npx project-translator-cli --help
```

## Common Commands

```bash
npx project-translator-cli translate project --workspace . --config project.translation.json
npx project-translator-cli translate files --workspace . --config project.translation.json --lang zh-cn
npx project-translator-cli config list --workspace . --config project.translation.json --json
npx project-translator-cli config validate --workspace . --config project.translation.json
```

Repository: `https://github.com/Project-Translation/project_translator`
